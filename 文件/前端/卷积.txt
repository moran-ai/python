1.卷积神经网络（CNN）由输入层、卷积层、激活函数、池化层、全连接层组成，即INPUT-CONV-RELU-POOL-FC。


2.卷积层：用它来进行特征提取。
Convolution

3.池化层：对输入的特征图进行压缩，一方面使特征图变小，简化网络计算复杂度；一方面进行特征压缩，提取主要特征。
Pooling 

4.全连接层：连接所有的特征，将输出值送给分类器（如softmax分类器）。


flat 扁平